[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect - redirect all non-file requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variable configuration for the build
[build.environment]
  NODE_VERSION = "22"
